# AGENTS.md

## Cursor Cloud specific instructions

This is an empty greenfield repository named **collox** (MIT licensed). As of the initial commit, the repo contains only `README.md` and `LICENSE` — no source code, dependencies, build system, or services exist yet.

- **No dependencies to install.** No `package.json`, `requirements.txt`, `go.mod`, `Cargo.toml`, `pyproject.toml`, or similar dependency files are present.
- **No services to run.** No application code, Dockerfiles, or docker-compose files exist.
- **No tests or linters configured.** No test frameworks or linting tools are set up.

Once application code and dependency files are added, this section should be updated with setup, build, test, and run instructions.
